balive:
  host: "${env.LIVESITE}"
  ssh:
    tty: 0
  root: /var/www/drupal/web
  uri: "https://${env.LIVESITE}"
  user: webadmin
# You'll need to have ssh-copy-id 'd your own ssh keys to webadmin@${env.LIVESITE}
# if yours is not the Ansible controller that provisioned the BAPC server initially
